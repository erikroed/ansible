- hosts: localhost
  gather_facts: true

  roles:
    - role: discord
      tags: discord
    - role: telegram
      tags: telegram
    - role: spotify
      tags: spotify
    - role: dotfiles
      tags: dotfiles

  ignore_errors: yes
