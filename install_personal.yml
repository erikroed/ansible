- hosts: localhost
  gather_facts: true

  roles:
    - role: discord
      tags: discord
    - role: telegram
      tags: telegram
    - role: spotify
      tags: spotify
    - role: dotfiles
      tags: dotfiles
    - role: sdkman
      vars:
        sdk_profile_personal: true
      tags: sdkman

  ignore_errors: yes
