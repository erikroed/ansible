- hosts: localhost
  become: true
  pre_tasks:
    - name: Update cache
      apt:
        update_cache: true
      tags:
        - node
        - discord
  tasks:
    - include_tasks: tasks/zsh.yaml
    - include_tasks: tasks/node.yaml
    - include_tasks: tasks/dotfiles.yaml
